#include <iostream>
#include <cstring>
//sa se aloce 4 variabile de tip int si sa se livreze memoria corespunzator
using namespace std;


int main()
{
    int a,b,c,d,*p1,*p2,*p3,*p4;
    *p1=malloc(a*sizeof(int));
    *p2=malloc(b*sizeof(int));
    *p3=malloc(c*sizeof(int));
}
